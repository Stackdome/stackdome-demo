# todo-app

Tiny Vite + React + TypeScript todo list used as a fixture for recorded demo walkthroughs.
No backend: todos live in `localStorage`.
Run with `npm install && npm run dev`, then open http://localhost:5173.
See [AGENTS.md](AGENTS.md) for selectors, state, and demo flows.
